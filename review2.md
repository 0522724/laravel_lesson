# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
- `<input type="hidden" name="_method" value="PUT">`

### findメソッドの引数に指定しているIDは何のIDか
- 編集や削除したいcontentカラムの内容に紐づく主キーとなるID

### findメソッドで実行しているSQLは何か
- 主キーを使用して紐づくレコードを取得するメソッドなので、SELECT * FROME テーブル名 WHERE 主キーカラム名　= 値(今回はSELECT * FROME todos WHERE id = {$id})

### findメソッドで取得できる値は何か
- 引数に渡された主キーであるIDと一致するレコード(Todoのインスタンス)

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
- 主キーが存在しない(新しい投稿)値に対してsaveメソッドを呼び出すとINSERT文が実行され、主キーが存在する値に対してsaveメソッドを呼び出すとすでにDBにあると認識されUPDATA文が実行される。


## Todo論理削除

### traitとclassの違いとは
- traitはクラスにプロパティやメソッドを追加するための機能でクラスの継承と違い、1つのクラスに複数のトレイトを追加することができる。トレイト自体はインスタンス化はできない。

### traitを使用するメリットとは
- 複数のクラス間でコードを共通化・再利用が可能になる。管理しやすい。複数追加できる。


## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
- TodoControllerクラスがインスタンス化されるタイミングなので、ユーザーからのリクエストがあったタイミング

### RequestクラスからFormRequestクラスに変更した理由
- バリデーション機能を実装するために、その基本となるクラスのFormRequestクラスに変更した

### $errorsのhasメソッドの引数・返り値は何か
- 引数は入力欄のname属性で返り値はtrueかfalse(bool型)

### $errorsのfirstメソッドの引数・返り値は何か
- 引数は入力欄のname属性で返り値はその入力欄で最初に発生したエラーメッセージ(文字列型)

### フレームワークとは何か
- アプリやシステムを効率よく作るための、ひな型・土台・仕組み」のこと。 開発に必要な機能や構造があらかじめ用意されているセットのこと。

### MVCはどういったアーキテクチャか
- DBとのやり取りを司るModel、ユーザーに表示させる画面の生成を司るView、ユーザーの入力に基づいてModelとViewの制御を司るController、ユーザーからのリクエストに応じてどの処理を動かすのかを司るRoutingと役割を分けて整理するアーキテクチャ

### ORMとは何か、またLaravelが使用しているORMは何か
- ORMはオブジェクトとしてDBを扱う技術。プログラミング言語のClassとデータベースのテーブルを関連付け(マッピング)することでSQLを直接操作することなくデータベースとマッピングされたClassのメソッドを用いることでDBとやり取りを行う。Laravelが使用しているORMはEloquent。

### composer.json, composer.lockとは何か
- composer.jsonはインストールするパッケージやライブラリを管理するファイル。プロジェクトのルートディレクトリに作成する。チーム開発を開始するとき、共有するcomposer.lockを作成する際に作成・編集する。
- composer.lockはインストールしたパッケージやライブラリを管理するファイルでcomposer installコマンドを実行したときに、プロジェクトのルートディレクトリに自動生成される。composer.lockには、composer.jsonより詳細な情報が記載されている。チーム開発の時は、composer.lockを共有する。

### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
- vendorディレクトリ

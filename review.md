# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
- テーブル内のレコードを全件取得しているので、SELECT * FROM テーブル名(今回はtodos)。

### Todoモデルのallメソッドの返り値は何か
- Illuminate\Database\Eloquent\Collectionクラスのインスタンス

### 配列の代わりにCollectionクラスを使用するメリットは
- メソッドやプロパティを使用することができ、ループや条件処理、変換などを簡単に行える。

### view関数の第1・第2引数の指定と何をしているか
- 第1引数に画面に表示したいbladeファイルを指定し、第2引数に渡したいデータを連想配列で指定して、Controllerで取得したデータををHTML(bladeファイル)に渡している。

### index.blade.phpの$todos・$todoに代入されているものは何か
- $todoにはTodoModelをインスタンス化した値(今回はtodosテーブルのレコード情報)が代入され、$todosにはtodosテーブルから取得した全レコードが代入されている。

## Todo作成機能

### Requestクラスのallメソッドは何をしているか
- フォームから送信された情報を配列で一括で取得している

### fillメソッドは何をしているか
- 上記allメソッドで取得してきた連想配列の値をTodoインスタンスの各プロパティに一括で代入している。

### $fillableは何のために設定しているか
- 一括代入には脆弱性があるため、$fillableを定義して代入できる項目に制限をかけている。

### saveメソッドで実行しているSQLは何か
- 取得して各プロパティに代入したデータを実際にデータベースに挿入するのがこのsaveメソッドになるので、INSERT INTO テーブル名 (カラム１, カラム２,....) VALUES (カラム１の値, カラム２の値….);。今回は$fillabelで制限がかかっているので、INSERT INTO todos (contents) VALUES ('フォームから送信された内容');。

### redirect()->route()は何をしているか
- 保存が終わったら、todo.index という名前のルート（一覧表示画面）にリダイレクトするようにしている。

## その他

### テーブル構成をマイグレーションファイルで管理するメリット
- SQLを知らなくても、PHPコードでテーブル操作ができるため学習コストが不要であることと、現在のデータベースの状態を他の開発者に共有することができる

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
- php artisan migrateコマンドを実行した時にup()が呼び出され、php artisan　migrate:rollbackを実行したときにdown()が呼び出される。

### Seederクラスの役割は何か
- レコードの作成を担っている

### route関数の引数・返り値・使用するメリット
- route関数の引数はweb.phpのRouteクラスで定義したルート名で、返り値は引数に渡したルート名が付いたURL(文字列)。ルートのURLが変わってもコード修正が少なくて済む、名前で管理できるので、ルートの整理がしやすい、複数箇所で使うリンクが一括で修正できるので保守性が高いなどのメリットがある。

### @extends・@section・@yieldの関係性とbladeを分割するメリット
- @extends()の記述で、継承する親Bladeを指定し、継承先の子Bladeの@section() ~ @endsectionで囲われた部分を、親Bladeの@yield()の部分に挿入している。
引数に同じ文字列を指定することで、@section()と@yield()を紐づけている。
共通部分を別Bladeに分割することによって、修正などが発生した場合この分割したファイルだけ修正すれば良いので、管理のしやすさと保守性を維持しやすくなる。

### @csrfは何のための記述か
- CSRF対策としてフォーム送信時にトークンを付与してサーバ側で検証する仕組みを用意している。

### {{ }}とは何の省略系か
- 変数や関数をHTMLで出力する際にXSSなどの対策で記載するエスケープ処理の文法の省略系。